```
优化后的 Auto.js 生活费管理 App 开发需求 Agent 提示词

请根据以下详细需求，为我开发一个 Auto.js 脚本应用，用于管理个人生活费分配与流水记录。应用需实现以下功能，并具有清晰的界面和本地持久化存储。

一、应用概述

这是一个专为学生设计的个人财务管理工具，将每月生活费划分为“校园卡”和“活动资金”两大模块，支持流水记录、余额修正、月末结转，并统计因节省而产生的“存款贡献”。用户需每月输入生活费总额，系统自动计算校园卡充值金额和活动资金初始金额。校园卡余额可持续跟踪，超出标准的部分会直接计入下月活动资金。

二、数据模型与核心概念

1. 每月基础数据：
   · 生活费总额（用户手动输入）
   · 校园卡月标准（每月固定金额，可修改，修改需存储历史记录）
   · 校园卡当前余额（持续跟踪）
   · 活动资金当前余额
   · 存款贡献累计金额（只增不减）
2. 三个独立模块的流水账：
   · 校园卡模块：记录充值（收入）和消费（支出）。
   · 活动资金模块：记录日常消费（支出）和可能的收入（如临时补贴）。
   · 存款贡献模块：只记录转入（收入），不记录支出。该模块表示用户从每月生活费中节省下来并存入的金额，其支出不影响日常生活，因此无需跟踪。
3. 流水记录通用结构：
   · 类型：收入 / 支出（存款贡献模块只有收入）
   · 金额
   · 分类（如餐饮、购物、交通等，用户可自定义）
   · 备注
   · 时间戳
4. 余额修正：当实际余额与系统记录不一致时，允许用户手动调整余额，并指定差额计入哪个模块的收入或支出，生成一条修正流水。

三、每月流程与计算逻辑

1. 月初初始化（开启新月）

用户通过两种方式之一触发“开启新月”流程：

· 方式一（自动计算）：在月末结转完成后，系统自动进入“下月初始化”页面，展示计算出的本月校园卡应充值金额和活动资金初始金额，用户确认后可直接开启新月。
· 方式二（手动开启）：用户随时可手动点击“开始新月”按钮，进入初始化页面，自行输入本月生活费总额和校园卡充值金额，系统根据输入实时计算活动资金。

无论哪种方式，最终都会弹出一个“新月确认”窗口，包含以下内容：

· 本月生活费总额（输入框，默认空或上月值）
· 本月校园卡充值金额（输入框，方式一下自动填充计算值，方式二为空）
· 实时计算显示：本月活动资金初始金额 = 生活费总额 − 校园卡充值金额
· 一个向右滑动的滑块，用户必须将滑块滑到最右端才能点击“确认开启”按钮。

只有在滑块滑动并点击确认后，新月才正式开启。如果用户关闭弹窗、取消或未滑动滑块，则视为未开启新月，不产生任何数据变更。

初始化逻辑（正式开启后执行）：

· 读取上月校园卡余额 C_prev（首次使用则为 0）和本月校园卡标准 S。
· 计算建议充值金额 R_suggest = max(S - C_prev, 0)。方式一中该值自动填入充值金额输入框；方式二中用户可参考此值自行填写。
· 用户确认的充值金额记为 R_actual，它可以是任意非负数值。
· 计算本月活动资金初始金额：A = L - R_actual（L 为用户输入的本月生活费总额）。
· 生成一条校园卡充值流水（收入，金额 R_actual），更新校园卡余额：C_new = C_prev + R_actual。
· 生成一条活动资金初始化流水（收入，金额 A，分类可设为“月初初始化”），或直接设置活动资金余额为 A。
· 记录本月已初始化状态，当前月份切换为新月。

注意：用户输入的充值金额可以超过建议值，但系统应提示可能导致活动资金减少；也可以小于建议值，表示用户打算少充，将更多资金留作活动资金。

2. 月中使用

· 用户可随时在校园卡或活动资金模块添加收入/支出流水，系统自动更新对应余额。
· 支持流水分类、备注、时间修改。

3. 月末结转

· 用户进入“月末结转”页面。
· 系统显示当前活动资金余额 A_current。
· 用户点击“结转”后，系统自动：
  · 将活动资金余额清零，生成一笔支出记录（分类可设为“结转至存款贡献”），金额为 A_current。
  · 在存款贡献模块生成一笔收入记录，金额同为 A_current，表示节省下来的存款增加。
  · 记录本月已结转状态。
· 结转完成后，系统提示用户可选择“立即开启下月”（进入方式一初始化）或稍后手动开启。

4. 校园卡标准修改

· 用户可在设置中修改校园卡月标准。
· 每次修改需存储一条历史记录（包含修改时间、旧标准、新标准）。
· 修改仅对下一个未开始的月份生效，当前已初始化的月份不受影响。修改时明确提示“本次修改将于下月生效”。

四、界面要求

1. 主界面（总览）

· 显示当前月份、校园卡余额、活动资金余额、存款贡献累计。
· 显示本月生活费总额、校园卡标准、本月充值金额。
· 提供按钮：添加流水、月末结转、开始新月、设置。

2. 流水记录界面

· 顶部两个 Tab：收入、支出（存款贡献模块不显示支出 Tab）。
· 模块选择：校园卡、活动资金、存款贡献（可通过下拉或标签切换）。
· 列表展示流水，每条显示分类、金额、备注、时间。
· 支持点击某条流水进行编辑或删除。
· “添加流水”按钮，弹出表单：金额、分类（可自定义预设）、备注、时间（默认当前）。

3. 余额修正界面

· 选择模块（校园卡、活动资金、存款贡献）。
· 显示系统当前余额和实际余额输入框。
· 差额自动计算，并让用户选择差额类型（收入/支出）和备注。
· 确认后生成一条修正流水并更新余额。

4. 图表展示

· 在总览页或独立页面，以饼图展示本月支出分类占比（活动资金或校园卡消费）。
· 可用列表形式展示各分类支出金额。
· 可切换模块和时间范围（本月、全部）。

5. 校园卡标准历史

· 设置页面中可查看标准修改历史（列表）。

五、技术要求

· 使用 Auto.js 的 UI 构建界面，支持点击、输入、列表、滑块等组件。
· 数据持久化：使用 Auto.js 的 storages 或文件存储（如 JSON 文件）保存所有数据，包括每月记录、流水、设置等。
· 所有日期使用本地时间，月份格式 YYYY-MM。
· 图表可使用简单 Canvas 绘制饼图，或使用第三方库（若 Auto.js 支持）。
· 代码需模块化，注释清晰，便于后续修改。

六、示例场景验证

假设用户首次使用：

· 输入生活费 2000，校园卡标准 1200。
· 开启新月：建议充值 1200，活动资金自动计算为 800。
· 用户添加活动资金支出：餐饮 100，购物 50。
· 月末结转：活动资金余额 650 转为存款贡献。
· 下月初始化（方式一）：假设上月校园卡余额 200（即消费了 1000），新生活费 2000，标准仍 1200，则建议充值 1000，活动资金自动计算为 1000。用户滑动滑块确认后，生成充值 1000 和活动资金 1000 的记录。

假设下下月校园卡余额达到 1500（大于标准 1200）：

· 开启新月时建议充值 0，活动资金 = 2000 - 0 = 2000，全部生活费进入活动资金。

七、关键注意事项

1. 活动资金计算必须为 A = 生活费总额 - 实际充值金额，不得引入补贴或多余调整。
2. 存款贡献模块只允许收入流水，不允许添加支出，余额只增不减。
3. 开启新月必须经过滑块确认，任何取消或未滑动滑块的行为都不能生效。
4. 两种开启新月方式必须都实现，且最终确认界面相同，仅充值金额的默认填充方式不同。
5. 校园卡余额可以超过标准，下月充值可为 0，这是正常情况，不应强制充值。

请根据以上需求，生成完整的 Auto.js 脚本代码，并确保代码可直接运行。
```